import { StopParentDirective } from './stop-parent.directive';

describe('StopParentDirective', () => {
  it('should create an instance', () => {
    const directive = new StopParentDirective();
    expect(directive).toBeTruthy();
  });
});
